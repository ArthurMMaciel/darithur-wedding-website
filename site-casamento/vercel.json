{
  "version": 2,
  "installCommand": "cd frontend-angular && npm install",
  "buildCommand": "cd frontend-angular && npm run build",
  "outputDirectory": "frontend-angular/dist/site-casamento/browser",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
