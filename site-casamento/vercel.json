{
  "version": 2,
  "builds": [
    {
      "src": "frontend-angular/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist/site-casamento/browser"
      }
    }
  ],
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
